{
  "name": "email-ingestion-lambda",
  "version": "1.0.0",
  "description": "Process inbound raw email files from S3 and push payloads to SQS",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.662.0",
    "@aws-sdk/client-sqs": "^3.662.0",
    "mailparser": "^3.9.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/mailparser": "^3.4.6",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
